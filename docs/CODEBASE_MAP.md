# L4L0 — Codebase Map

Rebuild in progress from Phase 0 on branch `project-lalo`. This map is
regenerated (via the `cartographer` skill) once real structure exists — see
`CLAUDE.md` and the active plan for the phase order and the reference-first
build mandate.
