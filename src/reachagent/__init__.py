"""ReachAgent — Web/API authorization-and-vulnerability testing agent.

Confirmed, multi-hop attack chains found via a live reachability graph and
deterministic verification instead of LLM judgment. See
``docs/reachagent-final-plan.md`` (v1.4, locked) for the full design.

This package is Phase 1 scaffolding (plan §15): structure only, no behavior.
"""

__version__ = "0.1.0"
