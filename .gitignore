# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.env

# Local chat-log dumps — not source
reachagentchat/

# Durable debug/crash log (logging_setup.py) — not source
logs/

# Runtime provider configs (contain API keys)
config/providers.json

# Cross-engagement pattern memory — local runtime state, not source
data/pattern_db.jsonl
.playwright-cli/
output/playwright/
.playwright-mcp/

# Local Claude Code tooling config — developer-machine settings, not project source
.claude/

# Scan-tool resume/state files (feroxbuster, etc.) accidentally left in a
# working directory during live testing — never source, may reference a
# live engagement's own scan state
*.state

# Vendored third-party corpora — not currently used by any shipped code;
# see THIRD_PARTY_NOTICES.md before ever re-adding one
/third_party/
# Added by code-review-graph
.code-review-graph/
